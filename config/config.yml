models:
  - type: main
    engine: openai
    model: gpt-4o-mini

rails:
  input:
    flows:
      - check jailbreak
      - check off-topic query
      - check prompt injection
  dialog:
    flows:
      - customer query flow
      - out of domain fallback
  output:
    flows:
      - check factual faithfulness
      - mask sensitive pii data
      - redact internal api keys
